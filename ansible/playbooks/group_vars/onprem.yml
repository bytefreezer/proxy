---
# ByteFreezer Proxy - On-Premises Deployment Variables
# On-prem deployment on tp2 (192.168.86.102)

# Account created: 2025-11-12
# Account Name: OnPrem Test Account
# Email: onprem@test.local
# Deployment Type: on_prem

# Account configuration for on-prem deployment
config:
  account_id: "s2jeovtia7zn"  # On-prem account (deployment_type: on_prem)
  bearer_token: "onprem-92643688d4e31fbc4ca03c8cb87ef6c7"  # On-prem account API key

  # Control plane is shared - still on tp3
  control_url: "http://192.168.86.103:8082"  # Control on tp3 (shared)

  # On-prem receiver on tp1 instead of managed receiver on tp2
  receiver:
    base_url: "http://192.168.86.101:8080/{tenantid}/{datasetid}"  # Receiver on tp1
