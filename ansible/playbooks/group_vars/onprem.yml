---
# ByteFreezer Proxy - On-Premises Deployment Variables
# On-prem deployment on tp2 (192.168.86.102)

# Account created: 2025-11-12
# Account Name: OnPrem Test Account
# Email: onprem@test.local
# Deployment Type: on_prem

# Override specific config values for on-prem deployment
# NOTE: These use the config_ prefix to avoid replacing the entire config dict
config_account_id: "s2jeovtia7zn"  # On-prem account (deployment_type: on_prem)
config_bearer_token: "onprem-92643688d4e31fbc4ca03c8cb87ef6c7"  # On-prem account API key
config_control_url: "http://192.168.86.103:8082"  # Control on tp3 (shared)
config_receiver_base_url: "http://192.168.86.101:8080/{tenantid}/{datasetid}"  # Receiver on tp1
