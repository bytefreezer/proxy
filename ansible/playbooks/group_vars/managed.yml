---
# ByteFreezer Proxy - Managed Deployment Variables
# This is the default managed deployment on tp1, tp2, tp3

# Override specific config values for managed deployment
# NOTE: These use the config_ prefix to avoid replacing the entire config dict
config_account_id: "ejq73vgnw26p"  # Managed account (deployment_type: managed)
config_bearer_token: "-6O9pIT7fVrIr715CIwIUgZuBkY7lB5sRYHx"  # Managed account API key
config_control_url: "http://192.168.86.103:8082"  # Control on tp3
config_receiver_base_url: "http://192.168.86.102:8080/{tenantid}/{datasetid}"  # Receiver on tp2
