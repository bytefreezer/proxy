  Need to explain in readme that this is required for all udp based plugins, setting is global on udp:

  #udp buffer size, make sure it matches your buffere size
  sysctl -w net.core.rmem_max=<value>
  sysctl -w net.core.rmem_default=<value>

